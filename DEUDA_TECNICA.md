# Deuda Técnica — REPORTA WEB v3

Registro de decisiones diferidas, workarounds conocidos y bloqueadores que no pudieron resolverse en el momento.

---

## Activa

| ID | Descripción | Detectada en |
|----|-------------|--------------|
| DT-TRIAL-02 | **Cache de trial_status en middleware:** El check de trial en `utils/supabase/middleware.ts` hace 2 queries DB en cada request autenticado (profiles + companies). Implementar cookie de corta duración `rw3_trial_status` (similar a `rw3_bloqueadas`) para cachear el estado del trial por sesión y evitar el overhead por request. | 2026-06-02 (v3.10.36) |
| DT-TRIAL-01 | **Configuración de trials en /sistema:** Los días de envío de correos `[1, 2, 4, 6, 8, 10]` y el contenido de cada email están hardcodeados en `app/api/cron/trial-emails/route.ts` y `lib/trial-email-templates.ts`. Implementar una pantalla en `/sistema/trial-config` donde el `reporta_admin` pueda: (1) configurar el día de envío de cada email, (2) editar el asunto y el cuerpo de cada correo, (3) activar/desactivar días individuales. Requiere: tabla `trial_email_config` en DB, server actions CRUD, UI con editor de texto enriquecido. | 2026-06-02 (v3.10.36) |
| DT-MIG03 | **Regenerar PDFs faltantes en reportes_maquinaria:** 1,414 registros tienen `pdf_url = NULL` porque nunca se generó PDF en Bubble (campo era opcional o el operador no tenía conectividad). Distribución: 2026 (138), 2025 (448), 2024 (415), 2023 (293), 2022 (73), 2020 (47). CISE: 579, GRUAS: 835. Evaluar si es viable regenerarlos vía la API de Gotenberg usando los datos ya migrados en Supabase. Consulta de identificación: `SELECT id, bubble_id, tenant_id, fecha_reporte FROM reportes_maquinaria WHERE tenant_id IN ('1cb97ec7...','6f4c923a...') AND pdf_url IS NULL`. | 2026-05-23 (v3.7.44) |
| DT-MIG02 | **Remediación post-migración Bubble→Supabase (3 bugs):** (1) `reportes_usuario.created_by` contiene Bubble IDs en registros donde el lookup de perfil falló — fix: `UPDATE reportes_usuario SET created_by = NULL WHERE created_by IS NOT NULL AND created_by NOT LIKE '%-%-%-%-%'`. (2) `reportes_maquinaria.foto_actividad_url` nunca migrada a Storage — fix: `BUBBLE_API_URL=https://reporta.la/api/1.1/obj npx tsx scripts/migrate-files-to-storage.ts --table=reportes_maquinaria_foto --step=migrate --concurrency=8`. (3) `reportes_usuario.pdf_url` nunca migrada a Storage (script `migrate-user-reports-files.ts` no estaba en el orquestador) — fix: `BUBBLE_API_URL=https://reporta.la/api/1.1/obj npx tsx scripts/migrate-user-reports-files.ts`. **Pre-requisito:** copiar datos de Bubble producción a Bubble test y ejecutar contra `version-test` primero. Los 3 scripts ya fueron corregidos en el código. | 2026-05-22 (v3.7.43) |
| DT-DNS01 | DNS `reportar.app` no apunta correctamente a Vercel. Error 522 (Cloudflare timeout) al acceder a `reportar.app` y `www.reportar.app`. Fix en Cloudflare: (1) registro A `@` → `76.76.21.21` con proxy desactivado, (2) CNAME `www` → `reportar.app` con proxy activado + Page Rule redirect `www.reportar.app/*` → `https://reportar.app/$1`, (3) verificar que `reportar.app` esté agregado en Vercel → proyecto `reportaweb3` → Settings → Domains. | 2026-05-22 (v3.7.41) |
| DT-MIG01 | Migración Bubble→Supabase incompleta en contactos de terceros: se migraron los contactos pero faltan sus catálogos de `cargo` y `area` (`contactos_cargo` y `contactos_area`). Sin los catálogos, los campos cargo/área de los contactos existentes quedan vacíos y los dropdowns en `ContactoDialog` no muestran opciones. Fix: seed de `contactos_cargo` y `contactos_area` con los valores únicos presentes en los contactos migrados. | 2026-05-13 (v3.7.23) |
| DT-PDF01 | Verificar que los templates PDF en web coincidan exactamente con los formatos físicos del cliente. Revisar `lib/reporte-personal-pdf-template.ts` vs G.PAC-02 (Reporte de Personal, 1 hoja) y `lib/informe-formato-pdf-template.ts` vs G.PAC-04 (Reporte de Alquiler de Maquinaria y Equipos, 2-4 hojas según configuración). El usuario compartió ejemplos reales del cliente GRUAS DEL PACÍFICO como referencia. | 2026-05-14 (v1.6.0) |

---

## Resuelta

| ID | Descripción | Resuelta en |
| DT-SuiteV2 | Suite E2E v2 multi-rol completa: 1032/1041 (99.1%) en 3 roles (planner/admin/viewer), 0 fallos, 9 skipped intencionales. `playwright.full.config.ts` sin filtro `@roles`. Fixes flows 02-05 (getMaquinarias `'-'` fallback + replaceIntervalsAsAdmin fecha derivation) cubrieron también la suite v2 sin cambios adicionales. | 2026-05-11 (v3.7.18) |
| --- | --- | --- |
| DT-R01 | `storageState: undefined` no anula project-level storageState en Playwright — fix: `storageState: { cookies: [], origins: [] }` | 2026-04-26 |
| DT-R02 | `tests/auth.setup.ts` legacy eliminado; reemplazado por `tests/auth/setup-{planner,admin,viewer}.ts` | 2026-04-26 |
| DT-R03 | `searchParams` síncrono en `/ventas/valoraciones` — fix: `Promise<ValoracionesParams>` + `await searchParams` | 2026-04-26 |
| DT-R04 | Upload `imagen_banco` / `firma` en `/settings/cotizaciones` — fix: eliminado `pathStart` hardcodeado; la action resuelve el tenant automáticamente | 2026-04-26 |
| DT-002 | Suite E2E multi-rol bloqueada — fix: `global-setup.ts` con `supabase.auth.admin.createUser()` crea los 3 usuarios E2E automáticamente; 6 `test.fixme` activados; `.env.test` con emails y passwords por rol | 2026-04-26 |
| DT-003 | `middleware.ts` deprecado en Next.js 16 — fix: renombrado a `proxy.ts` (convención Next.js 16); build limpio sin warnings | 2026-04-26 |
| DT-004 | 5 fixes planificación: fecha UTC -1 día, tarea multi-día en todos sus días, personal EXTERNO ocupado en timeline, badge color, nombre proveedor en timeline. Además: timeline Personal/Maquinaria derivado via `useMemo` desde `tareas` (reemplaza `getAvailability`), filtro proveedor al seleccionar EXTERNO, React key duplicada en `planificacion-table.tsx`. Middleware `/api` añadido a `PUBLIC_PATHS` para que crons no sean redirigidos a `/login`. | 2026-05-02 (v3.6.4) |
| DT-F1-9 | Fases 1, 2, 3, 4, 8, 9 (Oleadas 1 y 2): Informe Personal con festivos + auto-calc horas Ley Peruana + descanso compensatorio; Informe Maquinaria con auto-calc (duracionJornada + recorrido); PDF Personal y Maquinaria vía Gotenberg (API routes + storage `formatos`); festivos en timeline Planificación y datepicker nueva tarea; Settings Notificaciones EPP (`epp_notificar_observaciones_a`); botones PDF en tarea-detail-dialog. | 2026-05-03 (v3.7.1) |
| DT-F5 | Fase 5 Checklist web: formulario SI/NO/NO_APLICA; campo observación obligatorio al marcar NO; puntaje auto-calculado (count(SI) / (total−count(N/A)) × 100) guardado en DB; planes de acción creados automáticamente por cada NO con origen=CHECKLIST; secciones medidores/observaciones/declaración condicionales por config_checklist tenant. Fix PGRST201 FK ambigua formatos_versiones→formatos. Botón "Llenar" en tabla plantillas. Búsqueda de tarea por código en NuevoInformeForm (debounce 350ms, muestra cliente/sitio/fechas). | 2026-05-03 (v3.7.2) |
| DT-UAT1 | Dialog tarea planificación: scroll corregido (min-h-0); tabs reordenados Paso 1→2→3 (Paso 3 bloqueado sin inspección); títulos en sub-dialogs de reporte; selector personal filtrado a asignados de la tarea; validación solapamiento de jornadas (alerta + bloqueo submit). Firma cliente eliminada de web. Timeline maquinaria: fallback proveedor_id para tareas con maquinaria_id=null (bug createTarea, resuelto). createTarea: siempre guarda maquinaria_id para máquinas de catálogo. Búsqueda tareas por código Y título. | 2026-05-03 (v3.7.3) |
| DT-005 | Fase 6 PDF Checklist: `inspecciones_detalles` agrupados por categoría, renderizado vía Gotenberg, storage `formatos`, página `/inspecciones/[id]` con vista detalle. Fase 7 EPP: badges `estado_confirmacion` (PENDIENTE/PARCIAL/COMPLETO) en dashboard y página colaborador, panel observaciones con respuesta admin, botón Reabrir en informe RECHAZADO (InformeReadView). Planificación: botones lápiz en Recursos Asignados abren `EditRecursosDialog` con scope (este día / desde este día / todos los días); fix React Rules of Hooks en `tarea-detail-dialog`. | 2026-05-03 (v3.7.4) |
| DT-UAT2 | UAT planificación (Oleada 3): selector maquinaria filtrado a recursos asignados en `tarea-detail-dialog` (fallback lista completa si tarea sin recursos); detección solapamiento jornadas en `reporte-maquinaria-form` (alerta roja + bloqueo submit). App móvil: corrección payloads outbox → `app_asistencias`/`app_combustible`, `fecha_hora` ISO, `ubicacion_gps` JSON, jornadas descompuestas por columna, columnas correctas en `reportes_personal`/`reportes_maquinaria`. | 2026-05-03 (v3.7.6) |
| DT-E2E1 | Suite E2E: strict mode violations resueltas (`.first()` en locators `table tbody` y `h1`); `test.use({ storageState })` agregado a describe blocks sin pin de sesión (flows 02–05, 07, 13, 14); `createInspeccion` factory en `data-factory.ts` + cleanup (prerequisito del tab "Paso 3. Reportes" bloqueado sin inspección); `toHaveURL` eliminados en flow 08 (formatos/informes redirigen); flow 14 EPP pinned a admin con soft-check de botones; flow 17 viewer test actualizado. `goToToday()` con URL guard. Cargos: catálogo completo en `nueva-tarea-form` para perfiles sin `job_title_id`. | 2026-05-04 (v3.7.7) |
| DT-001 | Hydration mismatch Radix UI + React 19 en `DashboardHeader`: `DropdownMenu` generaba IDs distintos entre SSR (profile=null) y cliente (profile cargado). Fix: guard `mounted` en `components/dashboard/header.tsx` — el dropdown y datos de usuario solo se renderizan post-mount. Módulo Soporte agregado al menú de usuario. | 2026-05-04 (v3.7.8) |
| DT-Soporte | Módulo Soporte completo: tabla `tickets_soporte` + `tickets_soporte_respuestas` (migration `20260504100000`), función `next_ticket_soporte_numero` con `pg_advisory_xact_lock`, RLS por rol, bucket `problemas`; server actions (`lib/actions/soporte.ts`); UI: listado con filtros, formulario nuevo ticket con upload de imágenes (Ctrl+V paste), detalle con historial/respuesta/resolución/cierre. n8n responde vía `es_de_soporte=true`. Tests E2E flow 46. | 2026-05-04 (v3.7.8) |
| DT-Permisos | Módulo Permisos por Cargo completo: tabla `cargo_permisos` (migration `20260504000000`), VIED matrix UI en `/sistema/permisos`, server actions con upsert atómico; middleware cachea rutas bloqueadas en cookie `rw3_bloqueadas` (no-httpOnly); `UserProvider` lee la cookie para redirigir en cliente sin roundtrip. Tests E2E flow 45. | 2026-05-04 (v3.7.8) |
| DT-E2E2 | Fix post-cutover E2E flows 45+46: header titles para /soporte; `getJobTitles` acepta tenantId opcional; permisos page usa profile.tenant_id como fallback y auto-inicializa cargo_permisos cuando están vacíos (server + client); `createTicketViaDB` helper incluye entrada de sistema "Ticket creado"; locator `.first()` en navegación permisos; `toHaveCount` inválido removido. | 2026-05-05 (v3.7.9) |
| DT-E2E3 | Fix E2E 4to run: `getPageTitle('/soporte')` devolvía "Soporte" del menu (orden de checks en `header.tsx`); `locator('thead')` strict mode (4 secciones → `.first()`); `locator('tbody')` en cargo-change: `waitForTimeout(1000)` insuficiente para auto-seed → `waitForSelector({timeout:15000})`; `locator('h1')` strict mode en cotizaciones → `.first()`. 359/387 pasando (92.8%). | 2026-05-05 (v3.7.10) |
| DT-E2E4 | Fix E2E 5to run (21 fallos restantes): flow 43 INTERNO sin `createInspeccion` → tab Reportes disabled + Application error; flow 37 `/settings/sitios` sin defensive catch → fallo si action lanza; Grupo A buttons sin `data-testid` → `getByRole('main')` frágil → añadido `data-testid` en 4 botones planificación + PlanificacionPage usa locators por `data-testid`; Grupo B `toHaveURL(/epp/)` estricto → reemplazado por `not.toHaveURL(/login/)` + check condicional; Grupo C planner `/formatos` botón "Nueva plantilla" → test tolerante a redirect por cargo_permisos. Grupo F (4 fallos JWT) = infra, no fixable. | 2026-05-05 (v3.7.11) |
| DT-TZ | Timezone por tenant: columna `companies.timezone` (IANA, default `America/Lima`); middleware cachea zona en cookie `rw3_timezone` (httpOnly:false, maxAge 1h); `useUser().timezone` expuesto en `UserProvider`; `lib/utils/tz.ts` con `todayInTZ/nowTimeInTZ/buildLocalISO/formatInTZ`; `/settings/empresa` con selector de 37 zonas latam/centroam en 5 grupos. App móvil: `getCurrentTimezone()` en `lib/tenant.ts` con cache AsyncStorage; `llegada/salida/combustible/informe-personal` usan TZ correcta → timestamps con offset explícito (ej. `2026-05-05T14:30:00-05:00`). Fix bug `esDomingo()`: `new Date('YYYY-MM-DD')` era UTC medianoche → día anterior en Lima; fix: `new Date(fecha + 'T12:00:00Z')`. | 2026-05-06 (v3.7.12) |
| DT-AppTZ | Fix import `getCurrentTenantId` faltante en `app/informe-personal.tsx`; migration soporte idempotente (DROP TRIGGER/POLICY IF EXISTS); assets web (favicon/icon) agregados al repo. | 2026-05-06 (v3.7.13 / v1.5.1) |
| DT-Inspecciones | Módulo Inspecciones completo en sidebar: página listado `/inspecciones` con filtros (equipo, estado, búsqueda); `InspeccionesList` client component; sidebar "Gestión Formatos" con "Nueva Inspección" y "Inspecciones"; migration `20260506000000` agrega `/inspecciones` a `sistema_recursos` + seed `cargo_permisos` para todos los cargos activos. Fix logout `signOut({ scope: 'local' })` en sidebar para no revocar sesión global (evitaba que tests E2E fallaran post-logout). | 2026-05-06 (v3.7.14) |
| DT-E2E5 | Fix sesión E2E: Supabase rota refresh_token al primer uso → logout test cargaba `.auth/viewer.json`, rotaba su token, todos los tests viewer posteriores fallaban con `/login`. Fix: logout test usa `storageState: { cookies: [], origins: [] }` + login inline (sesión nueva en memoria, no toca archivos .auth). `global-setup.ts`: auto-crea `job_title` "E2E PLANNER", `profile_details` y `cargo_permisos` completos para el planner E2E. Suite v2 creada: `playwright.full.config.ts` sin `grep: /@roles/` → 1041 tests × 3 roles. v1: 380/387 (98.2%). v2: 1020/1041 (98%). Flows 02-05 pending rewrite (timeline maquinaria). | 2026-05-11 (v3.7.15) |
| DT-E2E6 | Fix flows 02-05 (timeline maquinaria): `ensureMaquinaria` insertaba `modelo: 'Test model'` → `getRecursosForPlanning` construía nombre `'Test model'` (join de campos) en lugar de `m.nombre` → test buscaba `'E2E_Grúa de test 001'` y no lo encontraba. Fix: eliminar `modelo` del insert → fallback a `m.nombre`. Fix middleware `updateSession`: copiar cookies al redirect con objeto explícito `{name,value,path,maxAge,...}` evita error de tipos en Next.js 16. Fix `setup-base.ts`: strip cookies `rw3_bloqueadas` y `rw3_timezone` del storage state para evitar permisos stale entre runs. Fix `45-permisos-cargo.spec.ts`: `afterAll` limpia `cargo_permisos` aunque un test falle a mitad. Wizard nueva tarea: `saveTareaBorradorBasic` guarda BORRADOR en Supabase al completar Step 1 y Step 2; `createTarea` acepta `header.id` para actualizar en vez de crear. | 2026-05-11 (v3.7.16) |
| DT-E2E7 | Fix flows 02-05 (2do round): root cause real era `getMaquinarias` que mapea `modelo = m.modelo_rel?.modelo \|\| m.modelo \|\| '-'` → `getRecursosForPlanning` usaba ese `'-'` (truthy) como display name; fix: `filter(v => v && v !== '-')` en lugar de `filter(Boolean)`. Fix `ensureMaquinaria`: al encontrar registro existente con `modelo` seteado, lo limpia via UPDATE. Fix `replaceIntervalsAsAdmin` (test flow 03): derivar `fecha_inicio`/`fecha_fin` del min/max de `fechas_multiples` (espejo de `createTarea`); actualizar aserción DB. 5/5 flows 02-05 pasando. | 2026-05-11 (v3.7.17) |
| DT-SVC01 | Servicios vacíos en selector de cotizaciones: `getServicios()` filtraba con `.eq('is_active', true)` pero registros migrados de Bubble tenían `is_active = NULL`. Fix: `.or('is_active.eq.true,is_active.is.null')` en `lib/actions/servicios.ts`. | 2026-05-20 (v3.7.29) |
| DT-PDF-MAQ | Informe Maquinaria APP: campos faltantes en `reportes_maquinaria` (autor, cliente, cotización venta/compra, proveedor, estado venta/compra, riggers, horas_alquiler) ahora se guardan correctamente. PDF auto-generado desde la APP via nuevo endpoint `POST /api/reportes-maquinaria/:id/generate-pdf` (autenticado con `X-Report-Service-Key`). Lógica PDF extraída a `lib/reporte-maquinaria-pdf.ts` compartida entre GET (browser) y POST (app trigger). Nuevo bucket Supabase Storage `reporte-maquinaria` con path `{tenant_slug}/{cliente_slug}/{year}/`. | 2026-05-20 (v3.7.27 / v1.8.10) |
